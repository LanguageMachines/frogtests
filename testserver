#!/bin/sh -x


$frog_bin/frog  --keep-parser-files=yes -S 7000  > testserver.log 2>&1  &
pid=`echo $!`

sleep 8

./frogclient1 &
./frogclient2 &

sleep 4
cat testserver1.out > testserver.out
cat testserver2.out >> testserver.out

kill $pid

